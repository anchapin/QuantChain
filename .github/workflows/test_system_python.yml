name: Test System Python
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Check Python
        run: |
          which python
          python --version
          which python3
          python3 --version
          pip --version
